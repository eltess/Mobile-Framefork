pipeline {
    agent any
    stages {
        stage('Setup parameters') {
            steps {
                script {
                    properties([
                        parameters([
                            choice(
                                choices: ['ui', 'api'],
                                name: 'RUN'
                            )
                        ])
                    ])
                }
            }
        }

        stage('Test') {
            steps {
                sh clean test -pl=api-rozetka-tests  -Dsurefire.suiteXmlFiles=src/test/resources/test-suites/api-suit.xml
            }
        }
    }
}